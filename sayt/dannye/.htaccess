# Данные и резервные копии не должны открываться из браузера:
# menu.json отдаётся страницами, а не напрямую, а dostup.php хранит
# хеш пароля админки. Синтаксис Apache 2.2.
Order allow,deny
Deny from all
